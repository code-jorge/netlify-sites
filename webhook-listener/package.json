{
  "name": "webhook-receiver",
  "version": "1.0.0",
  "description": "Simple Netlify site with webhook receiver and blob storage",
  "type": "module",
  "scripts": {
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
